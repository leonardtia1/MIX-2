pipeline {
    agent any
    stages {
        stage('Checkout') {
            when {
                expression {
                    BRANCH_NAME == "dev"
                }
            }
            steps {
              echo "Cloning the source code........" 
            }
        }
    }
}
```
